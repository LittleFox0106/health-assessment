# 全栈挑战交付文档

**日期**: 2025年5月13日  
**项目名称**: 健康测评系统

---

## 1. 完整可演示的线上链接

### 1a. 公网可达的 URL

**演示地址**: https://health-idq6il3p2-littlefox0106s-projects.vercel.app/

**演示流程**（从头到尾走一遍 funnel）：
1. 打开演示地址，匿名完成 5 步测评（性别 → 目标 → 身体数据 → 运动频率 → 结果）
2. 在结果页可以看到未付费状态的预览内容（BMI、每日热量）
3. 点击付费按钮 → 弹出登录框 → 使用测试账号登录
4. 完成支付后查看完整报告（目标日期、进度曲线、个性化建议）

### 1b. /pay 接口的可重放调用方式（cURL）

```bash
# 基础 URL
BASE_URL="https://health-idq6il3p2-littlefox0106s-projects.vercel.app"

# 1. 创建匿名会话
SESSION_RESPONSE=$(curl -s -X POST "$BASE_URL/api/session")
SESSION_ID=$(echo $SESSION_RESPONSE | grep -o '"sessionId":"[^"]*"' | cut -d'"' -f4)
echo "Session ID: $SESSION_ID"

# 2. 完成测评（4步）
# 第1步：性别
curl -s -X PUT "$BASE_URL/api/quiz/progress" \
  -H "Content-Type: application/json" \
  -d "{\"sessionId\":\"$SESSION_ID\",\"step\":1,\"data\":{\"gender\":\"male\"}}"

# 第2步：目标
curl -s -X PUT "$BASE_URL/api/quiz/progress" \
  -H "Content-Type: application/json" \
  -d "{\"sessionId\":\"$SESSION_ID\",\"step\":2,\"data\":{\"goal\":\"lose_weight\"}}"

# 第3步：身体数据
curl -s -X PUT "$BASE_URL/api/quiz/progress" \
  -H "Content-Type: application/json" \
  -d "{\"sessionId\":\"$SESSION_ID\",\"step\":3,\"data\":{\"age\":28,\"height\":175,\"weight\":80,\"targetWeight\":70}}"

# 第4步：运动频率 + 计算
curl -s -X POST "$BASE_URL/api/quiz/calculate" \
  -H "Content-Type: application/json" \
  -d "{\"sessionId\":\"$SESSION_ID\",\"data\":{\"exerciseFreq\":\"moderate\"}}"

# 3. 查看未付费结果（预览）
curl -s "$BASE_URL/api/quiz/result?sessionId=$SESSION_ID"

# 4. 调用 /pay 接口进行支付
curl -X POST "$BASE_URL/api/pay" \
  -H "Content-Type: application/json" \
  -d "{
    \"sessionId\": \"$SESSION_ID\",
    \"planType\": \"monthly\",
    \"amount\": 29.99
  }"

# 5. 查看已付费结果（完整报告）
curl -s "$BASE_URL/api/quiz/result?sessionId=$SESSION_ID"
```

**价格表**（服务端硬编码验证）：
| 套餐类型 | 价格 | 说明 |
|----------|------|------|
| monthly | 29.99 | 月度订阅 |
| yearly | 199.99 | 年度订阅（省 44%） |
| lifetime | 499.99 | 终身会员 |

> **安全说明**: 服务端硬编码价格表，不信任前端传入的金额。如果 amount 与 planType 不匹配，接口返回 400 错误。

### 1c. 已支付的测试 sessionId

**测试账号（已付费）**：
- 邮箱: `vip@test.com`
- 密码: `123456`

**已支付的 Session ID**（可直接用于对比测试）：
```
sess_vip_7a8b9c0d1e2f3456789012345678
```

**付费前后对比示例**：

```bash
# 使用已付费账号的 sessionId 查看完整报告
curl "https://health-idq6il3p2-littlefox0106s-projects.vercel.app/api/quiz/result?sessionId=sess_vip_7a8b9c0d1e2f3456789012345678"
```

**未付费 vs 已付费响应对比**：

| 内容 | 未付费状态 | 已付费状态 |
|------|-----------|-----------|
| BMI | ✅ 可见 | ✅ 可见 |
| 每日热量 | ✅ 可见 | ✅ 可见 |
| 目标日期 | ❌ 锁定 | ✅ 可见 |
| 达成天数 | ❌ 锁定 | ✅ 可见 |
| 进度预测曲线 | ❌ 锁定 | ✅ 可见 |
| 个性化建议 | ❌ 锁定 | ✅ 可见 |

---

## 2. 代码仓库

**GitHub 链接**: https://github.com/LittleFox0106/health

### 如何启动项目

```bash
# 1. 克隆项目
git clone https://github.com/LittleFox0106/health.git
cd health-fix

# 2. 安装依赖
npm install

# 3. 配置环境变量
# 创建 .env.local 文件，填入以下内容：
```

**.env.local 配置**：
```env
# 数据库连接（Supabase PostgreSQL）
DATABASE_URL="postgresql://postgres:[password]@db.xxx.supabase.co:5432/postgres?pgbouncer=true"
DIRECT_URL="postgresql://postgres:[password]@db.xxx.supabase.co:5432/postgres"
```

```bash
# 4. 初始化数据库
npx prisma migrate dev
npx prisma db seed

# 5. 启动开发服务器
npm run dev

# 6. 访问 http://localhost:3000
```

### API 文档位置

- **API 文档**: `health-fix/API-DOC.md`
- **AI 使用复盘**: `health-fix/AI-使用复盘.docx`

---

## 3. 数据库 Schema 图

### 表结构关系

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              数据库 Schema 关系图                              │
└─────────────────────────────────────────────────────────────────────────────┘

┌──────────────┐         ┌──────────────┐         ┌──────────────────┐
│   Account    │◄────────┤     User     │◄────────┤   QuizSession    │
│   (账号表)    │   1:1   │   (用户表)    │   1:1   │   (测评会话表)    │
├──────────────┤         ├──────────────┤         ├──────────────────┤
│ id (PK)      │         │ id (PK)      │         │ id (PK)          │
│ email (UQ)   │         │ sessionId(UQ)│         │ userId (FK,UQ)   │
│ password     │         │ accountId(FK)│         │ currentStep      │
│ nickname     │         │ createdAt    │         │ isCompleted      │
│ createdAt    │         │ updatedAt    │         │ gender           │
│ updatedAt    │         └──────────────┘         │ goal             │
└──────────────┘              │                    │ age, height      │
                              │                    │ weight           │
                              │                    │ targetWeight     │
                              │                    │ exerciseFreq     │
                              │                    │ bmi              │
                              │                    │ dailyCalories    │
                              │                    │ targetDate       │
                              │                    │ createdAt        │
                              │                    │ updatedAt        │
                              │                    └──────────────────┘
                              │
                              │         ┌──────────────────┐
                              └────────┤   Subscription   │
                                 1:1   │   (订阅表)        │
                                       ├──────────────────┤
                                       │ id (PK)          │
                                       │ userId (FK,UQ)   │
                                       │ status           │
                                       │ planType         │
                                       │ startedAt        │
                                       │ expiresAt        │
                                       │ paymentId        │
                                       │ paidAmount       │
                                       │ paidAt           │
                                       │ createdAt        │
                                       │ updatedAt        │
                                       └──────────────────┘
```

### 表说明

| 表名 | 说明 | 核心字段 |
|------|------|----------|
| **Account** | 账号表，存储邮箱密码登录信息 | email, password, nickname |
| **User** | 用户表，基于匿名 Session，可绑定账号 | sessionId, accountId |
| **QuizSession** | 测评会话表，存储所有测评数据和计算结果 | currentStep, gender, goal, bmi, dailyCalories... |
| **Subscription** | 订阅表，管理会员状态和支付信息 | status, planType, paidAmount, paidAt |

### 关系说明

1. **Account : User = 1 : 1**（可选）
   - 匿名用户没有 Account
   - 登录后可绑定，实现数据迁移

2. **User : QuizSession = 1 : 1**
   - 每个用户只有一个测评会话
   - 删除用户时级联删除测评数据

3. **User : Subscription = 1 : 1**
   - 每个用户对应一个订阅记录
   - 存储会员状态和支付历史

---

## 4. AI 使用复盘

### 如何利用 AI 进行数据库建模

**实践方法**：
1. **需求描述**：向 AI 描述业务需求（用户系统、测评系统、支付系统的数据关系）
2. **Schema 生成**：AI 生成 Prisma Schema 草案，包含模型定义和关系映射
3. **架构审查**：使用 Claude/DeepSeek 审查 Schema 的合理性，检查范式问题
4. **迭代优化**：AI 根据反馈优化字段类型、索引设计和约束条件
5. **验证落地**：生成迁移脚本并验证数据库结构

**关键成果**：
- 完整的 Prisma Schema，包含 Account、User、QuizSession、Subscription 四个核心模型
- 支持软删除和数据隔离
- 合理的关联关系设计（1:1 关系，级联删除）

### 如何利用 AI 生成 Mock 数据

**实践方法**：
1. **模型输入**：提供数据模型结构和字段要求给 AI
2. **数据生成**：AI 生成符合业务逻辑的测试数据（如不同 BMI 范围的用户）
3. **Seed 脚本**：创建 Prisma Seed 脚本自动填充数据库
4. **数据验证**：验证数据完整性和关联关系

**关键成果**：
- `prisma/seed.ts` 脚本，自动创建测试账号和数据
- 付费/未付费测试账号（vip@test.com / normal@test.com）
- 完整的测评数据示例，支持全流程测试

### 如何利用 AI 编写复杂逻辑

**实践方法**：
1. **逻辑描述**：详细描述业务逻辑（健康评分算法、支付流程、数据隔离规则）
2. **代码框架**：AI 生成 TypeScript 代码框架，包含类型定义和错误处理
3. **交叉验证**：使用 AI 交叉验证 - Claude 审查算法正确性，DeepSeek 检查边界情况
4. **迭代优化**：根据测试结果调整评分权重和支付验证逻辑
5. **测试覆盖**：添加单元测试验证核心算法

**关键成果**：
- **评分计算 API**：BMI / BMR / TDEE 计算（Mifflin-St Jeor 公式）
- **支付处理流程**：服务端价格验证，防止篡改
- **数据绑定机制**：匿名/登录用户数据迁移，Session 隔离

### AI 工具矩阵

| AI 工具 | 主要职责 | 使用场景 |
|---------|----------|----------|
| **Trae (SOLO)** | 核心编程 IDE | 项目搭建、代码编写、功能实现、Skill 调用 |
| **Claude** | 架构审查 | 代码架构合理性验证、设计模式建议 |
| **DeepSeek** | 技术方案验证 | 技术选型评估、方案可行性分析 |
| **豆包** | UI/UX 优化 | 图片处理、界面优化建议 |

### 开发流程

1. 用 Brainstorming Skill 进行开发方案的生成
2. 针对开发方案进行审查，对于细节的开发方案进行调整
3. 交给 AI 按照开发方案进行开发
4. 粗略检查基本逻辑问题
5. 部署到 Vercel 上，利用 GitHub 和 Vercel 的联动进行运行
6. 通过 AI 之间交叉验证 Debug
7. 增添对于功能理解的细节，AI 进行实现
8. 检查每一步逻辑，同时读懂前期没理清的代码细节
9. 按照要求对代码和功能进行反复细化
10. 交付

---

## 附录：测试账号

| 角色 | 邮箱 | 密码 | 说明 |
|------|------|------|------|
| VIP 用户 | `vip@test.com` | `123456` | 已激活终身会员，可直接查看完整报告 |
| 普通用户 | `normal@test.com` | `123456` | 未付费，仅可查看报告预览 |

---

**文档结束**
